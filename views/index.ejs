<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        h1 {
    color:maroon;
}


.post{
    background-color: blanchedalmond;
}


.user{
    font-style: italic;
}
body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .header {
            background-color: #333;
            color: white;
            padding: 20px 0;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 2.5em;
        }
        .header p {
            margin: 0;
            font-size: 1.2em;
        }
    </style>
</head>
<body>
    <header>
            <div class="header">
        <h1>Welcome to Shoe Data</h1>
        <p>Your ultimate source for all shoe information</p>
    </div>
    </header>

     <a href="http://localhost:8080/posts/new">create new</a>
    <% for(post of posts){ %>
        
    <div class="post">
        <h3 class="user">@<%= post.username %></h3>
        <h4 class="post"><%= post.content %></h4>
       <hr>
       <a href="http://localhost:8080/posts/<%= post.id %>">details</a>
       <a href="http://localhost:8080/posts/<%= post.id %>/edit">edit</a>
       <form method="post" action="/posts/<%= post.id %>?_method=Delete">
        <button>Delete</button>
    </form>
    </div>
        <% } %>
</body>
</html>